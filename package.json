{
  "name": "terrier-housing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test:host": "vite --host localhost --port 4174 --strictPort",
    "prebuild": "mkdir -p public && cp firebase-config.js public/firebase-config.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
